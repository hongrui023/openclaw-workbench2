下面是一篇长文献中若干连续区块的结构化小结。请把它们【合并】成一份更紧凑的同类小结。

文献标题：{{document_title}}
合并分组：{{group_label}}

## 要求

1. 输出结构必须与输入保持一致，仍然是这六个小节，顺序不变：

   `## 研究背景` / `## 核心实验方法` / `## 关键实验结果`
   `## 主要结论` / `## 研究局限性` / `## 参考文献（本块出现的引用）`

2. 删除重复内容，但**保留全部具体信息**：数值、方法名、参数、结论、引用。
   合并时不要把"具体"合并成"笼统"。
3. 每一条仍保留来源页码标注，形如（第 N–M 页）。信息来自多处时可以合并页码区间。
4. **不得引入任何输入中没有的信息。**
5. 不同来源互相矛盾时，如实并列写出并分别标明页码，**不要强行统一**。
6. 不要写任何前言、总结语，不要使用一级标题（`#`）。
7. 如果某个小节在全部输入中都未涉及，写「未涉及」。

【输入小结开始】
{{summaries}}
【输入小结结束】
