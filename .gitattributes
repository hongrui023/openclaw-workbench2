# ============================================================
# openclaw-workbench · .gitattributes
#
# 目的：让仓库里的换行符统一为 LF。
#
# 为什么必须显式声明：
#   开发机是 Windows，git 默认会把文本文件的换行转成 CRLF 存进仓库。
#   而工作台真正运行的地方是 Linux（极空间 / 容器）。
#   一个带 CRLF 的 .sh 在 Linux 上执行会直接报
#   "bad interpreter: /bin/bash^M" —— 这类问题很费时间排查，
#   最好的办法是从源头杜绝，而不是出错后再回头改。
# ============================================================

# 默认：仓库内一律 LF，检出时也是 LF
* text=auto eol=lf

# Windows 专用脚本保留 CRLF（Windows 上对 CRLF 更友好）
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制：不做任何换行转换
*.pdf binary
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
